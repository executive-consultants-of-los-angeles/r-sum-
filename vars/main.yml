---
apt_install_list:
    - iputils-ping
    - net-tools
    - nmap
    - postgresql-client
    - vim
current_directory: "{{ lookup('pipe', 'pwd') }}"
dirs: "{{ lookup('pipe', 'ls -1 files/rsum/cvs/').split('\n') | list }}"
mdirs: "{{ lookup('pipe', 'ls -1 ../../files/rsum/cvs/').split('\n') | list }}"
pip_install_list:
    - django
    - psycopg2
    - 'git+https://github.com/executive-consultants-of-los-angeles/python-docx'
...
# vim: ft=ansible:
