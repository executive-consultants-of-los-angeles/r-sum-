---
dependency:
  name: galaxy
driver:
  name: vagrant
lint:
  name: yamllint
platforms:
  - name: mrsum 
    box: ubuntu/xenial64
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: vagrant
verifier:
  name: testinfra
  lint:
    name: flake8
...
# vim: ft=ansible:
