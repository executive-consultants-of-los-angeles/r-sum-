---
apt_install_list:
    - iputils-ping
    - net-tools
    - nmap
    - postgresql-client
    - vim
dirs: '{{ lookup('pipe', 'ls /src/rsum/files/rsum/cvs/') }}'
pip_install_list:
    - django
    - psycopg2
    - 'git+https://github.com/executive-consultants-of-los-angeles/python-docx'
...
# vim: ft=ansible:
