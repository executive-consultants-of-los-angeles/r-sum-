---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: "rsum"
    command: "/usr/bin/supervisord -n"
    image: ecla/ansible:latest
    ports:
      - "8192:8192"
    volumes:
      - "/srv/rsum:/srv/rsum:rw"
  - name: "psql"
    command: "/usr/bin/supervisord -n"
    image: ecla/psql:latest
    ports:
      - "5433:5432"
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
...
# vim: ft=ansible:
