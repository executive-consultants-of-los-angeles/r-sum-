---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: "mpsql"
    command: "/usr/bin/supervisord -n"
    image: ecla/psql:latest
    ports:
      - "5432"
  - name: "mrsum"
    command: "/usr/bin/supervisord -n"
    image: ecla/ansible:latest
    ports:
      - "8192"
  - name: "mngnx"
    command: "/usr/bin/supervisord -n"
    image: ecla/ansible:latest
    ports:
      - "80"
  - name: "mngos"
    command: "/usr/bin/supervisord -n"
    image: ecla/ansible:latest
    ports:
      - "80"
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
...
# vim: ft=ansible:
