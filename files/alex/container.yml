---
version: "unix:///var/run/docker.sock" 
settings:
  conductor_base: ecla/ansible:latest
services:
  arsum:
    name: "arsum"
    from: ecla/ansible:latest
    ports:
      - "8193:8193"
...
# vim: ft=ansible:
